package schwarz.jobs.interview.coupon;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Tests for the Coupon application context.
 * Verifies that the application loads correctly.
 */
@SpringBootTest
class CouponApplicationTests {

	/**
	 * Test that the application context loads without issues.
	 */
	@Test
	void contextLoads() {
		// This test checks that the Spring application context starts up correctly.
	}
}
